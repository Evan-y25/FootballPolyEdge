__pycache__/
*.pyc
.venv/
venv/
.env

# Third-party reference project (has its own git history)
ReferenceProject/

# Paper-trading state
paper_positions.json

# SQLite replay store
data/

# Evolution runtime logs (shown in UI; not version-controlled)
memory/

# Holds live env incl POLY_PRIVATE_KEY — never commit
ecosystem.config.js
.env
